export default class Emitter {
  static newInstance(name, instanceId, instance) {
    return new Emitter(name, instanceId, instance);
  }

  constructor(name, instanceId, instance) {

  }

  emit(type, msg) {

  }
}
